#!/usr/bin/env bash
set -euo pipefail

PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$PROJECT_DIR/bin/run-remote-sqlplus-sysdba" "$PROJECT_DIR/sql/02_list_candidate_schemas.sql"
